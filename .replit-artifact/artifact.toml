kind = "web"
previewPath = "/"
title = "FASTANET ISP Billing System"
version = "1.0.0"
id = "artifacts/fastanet"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20959

[services.development]
run = "pnpm --filter @workspace/fastanet run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fastanet", "run", "build" ]
publicDir = "artifacts/fastanet/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20959"
BASE_PATH = "/"
